<%= form_for(@supplier) do |f| %>
  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% @supplier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <font color="white"><%= f.label :name %></font><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <font color="white"><%= f.label :address %></font><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <font color="white"><%= f.label :phone %></font><br>
    <%= f.number_field :phone %>
  </div>
  <div class="field">
    <font color="white"><%= f.label :Contact_name %></font><br>
    <%= f.text_field :Contact_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
