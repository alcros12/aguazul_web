<%= form_for(@tipo) do |f| %>
  <% if @tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo.errors.count, "error") %> prohibited this tipo from being saved:</h2>

      <ul>
      <% @tipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_name %><br>
    <%= f.text_field :tipo_name %>
  </div>
  <div class="field">
    <%= f.label :tipo_des %><br>
    <%= f.text_field :tipo_des %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
